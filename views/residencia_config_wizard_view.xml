<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>

    <record id="view_asovec_residencia_config_wizard_form" model="ir.ui.view">
      <field name="name">asovec.residencia_config_wizard.form</field>
      <field name="model">asovec.residencia_config_wizard</field>
      <field name="arch" type="xml">
        <form string="Configuración de Residencias">
          <sheet>
            <group>
              <field name="proyecto_aso_ids" widget="many2many_tags"
                options="{'no_create': True}"/>
            </group>

            <group invisible="not file_data">
              <field name="file_name" invisible="1"/>
              <field name="file_data"
                filename="file_name"
                widget="binary"
                readonly="1"
                string="Archivo Excel generado"/>
            </group>

            <footer>
              <button name="action_generar" type="object" string="Generar" class="btn-primary"/>
              <button name="action_generar_excel" type="object" string="Generar Excel" class="btn-secondary"/>
              <button string="Cerrar" special="cancel" class="btn-secondary"/>
            </footer>
          </sheet>
        </form>
      </field>
    </record>

    <record id="residencia_config_wizard_action" model="ir.actions.act_window">
      <field name="name">Configuración de Residencias</field>
      <field name="res_model">asovec.residencia_config_wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

  </data>
</odoo>
