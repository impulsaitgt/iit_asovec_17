<?xml version="1.0"?>
<odoo>
    <!--Vista formulario -->
    <record id="residencia_form_view" model="ir.ui.view">
        <field name="name">residencia.form.view</field>
        <field name="model">asovec.residencia</field>
        <field name="arch" type="xml">
            <form string="Residencia">
                <header>
                    <button name="action_nueva_lectura" type="object" string="Nueva Lectura"
                        class="btn-primary" />
                    <button name="action_print_estado_cuenta_lecturas"
                        type="object"
                        string="Estado de Cuenta (Lecturas)"
                        class="btn-primary"
                        icon="fa-file-pdf-o" />
                    <button name="action_abrir_recibo_wizard"
                        type="object"
                        string="Recibo Mensual"
                        class="btn-primary"
                        icon="fa-file-pdf-o" />
                    <button name="action_abrir_recibo_detallado_wizard"
                        type="object"
                        string="Recibo Mensual Detallado"
                        class="btn-primary"
                        icon="fa-file-pdf-o" />
                    <button name="action_abrir_estado_cuenta_wizard"
                        type="object"
                        string="Estado de Cuenta"
                        class="btn-primary"
                        icon="fa-file-pdf-o" />
                </header>

                <sheet>
                    <field name="contador_count" invisible="1" />
                    <field name="lectura_count" invisible="1" />

                    <div class="oe_button_box" name="button_box">
                        <button type="object" name="action_ver_contadores" class="oe_stat_button"
                            icon="fa-tachometer">
                            <field name="contador_count" widget="statinfo" string="Contadores" />
                        </button>
                        <button type="object" name="action_ver_lecturas" class="oe_stat_button"
                            icon="fa-list">
                            <field name="lectura_count" widget="statinfo" string="Lecturas" />
                        </button>
                    </div>
                    <group>
                        <field name="name" placeholder="Nombre/Codigo" />
                    </group>
                    <group>
                        <group>
                            <field name="direccion" placeholder="Direccion" />
                            <field name="direccion_real" readonly="1" />
                            <field name="sector" />
                            <field name="calle" placeholder="Calle" />
                            <field name="no_casa" placeholder="No. Casa" />
                        </group>
                        <group>
                            <field name="cliente_id" />
                            <field name="proyecto_aso_id" />
                            <field name="activo" readonly="sin_contador" />
                            <field name="no_paga_servicios" />
                            <field name="sin_contador" readonly="no_paga_servicios" />
                            <field name="metros_especiales" />
                            <field name="metros_especiales_cantidad"
                                readonly="not metros_especiales" />
                            <field name="currency_id" invisible="1" />
                            <field name="cobro_base_especial" />
                            <field name="cobro_base_especial_valor"
                                readonly="not cobro_base_especial" />
                            <field name="exonera_exceso_agua" />
                        </group>
                    </group>
                    <group>
                        <field name="detalle" widget="text" placeholder="Escribe aquí..." />
                    </group>
                    <notebook>
                        <page string="Servicios con comportamiento especial para esta Residencia"
                            name="servicios_relacionados">
                            <field name="residencia_lines" mode="tree,kanban">
                                <tree editable="bottom" string="Servicios">
                                    <control>
                                        <create string="Agrega Servicio" />
                                    </control>
                                    <field name="producto_id" />
                                    <field name="currency_id" column_invisible="1" />
                                    <field name="precio"
                                        help="Deja en Cero si quieres excluirlo totalmente" />
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!--Vista
    de arbol-->
    <record id="residencia_tree_view" model="ir.ui.view">
        <field name="name">residencia.tree.view"></field>
        <field name="model">asovec.residencia</field>
        <field name="arch" type="xml">
            <tree string="Residencias">
                <field name="name" />
                <field name="direccion_real" string="Dirección" />
                <field name="sector" />
                <field name="calle" />
                <field name="no_casa" />
                <field name="detalle" />
                <field name="cliente_id" />
                <field name="proyecto_aso_id" />
                <field name="activo" />
                <field name="metros_especiales" optional="hide" />
                <field name="metros_especiales_cantidad" optional="hide" />
                <field name="cobro_base_especial" optional="hide" />
                <field name="cobro_base_especial_valor" optional="hide" />
            </tree>
        </field>
    </record>

    <!-- ===================== -->
    <!--  RESIDENCIA - SEARCH  -->
    <!-- ===================== -->
    <record id="asovec_residencia_search_view" model="ir.ui.view">
        <field name="name">asovec.residencia.search</field>
        <field name="model">asovec.residencia</field>
        <field name="arch" type="xml">
            <search string="Buscar Residencias">
                <!-- Quick search -->
                <field name="name" />
                <field name="direccion_real" string="Dirección" />
                <field name="cliente_id" />
                <field name="proyecto_aso_id" />
                <field name="sector" />
                <field name="calle" />
                <field name="no_casa" string="No. Casa" />
                <separator />
                <filter string="Activos" name="son_activos"
                    domain="[('activo','!=',False)]" />
                <filter string="Inactivos" name="no_son_activos"
                    domain="[('activo','=',False)]" />


                <separator />

                <!-- Filters -->
                <filter string="Con contadores" name="con_contadores"
                    domain="[('contadores_ids','!=',False)]" />
                <filter string="Sin contadores" name="sin_contadores"
                    domain="[('contadores_ids','=',False)]" />

                <separator />

                <filter string="Metro base propio" name="con_metro_base_propio"
                    domain="[('metros_especiales','=',True)]" />
                <filter string="Metro base de proyecto" name="sin_metro_base_propio"
                    domain="[('metros_especiales','=',False)]" />

                <separator />

                <filter string="Canon de agua propio" name="con_cobro_base_propio"
                    domain="[('cobro_base_especial','=',True)]" />
                <filter string="Canon de agua de proyecto" name="sin_cobro_base_propio"
                    domain="[('cobro_base_especial','=',False)]" />

                <separator />

                <!-- Group By -->
                <group expand="0" string="Agrupar por">
                    <filter string="Proyecto" name="group_proyecto"
                        context="{'group_by':'proyecto_aso_id'}" />
                    <filter string="Contacto" name="group_contacto"
                        context="{'group_by':'cliente_id'}" />
                    <filter string="Sector" name="group_sector"
                        context="{'group_by':'sector'}" />
                    <filter string="Calle" name="group_calle"
                        context="{'group_by':'calle'}" />
                    <filter string="Activo" name="group_activo"
                        context="{'group_by':'activo'}" />
                    <filter string="Metro base propio" name="group_metros_especiales"
                        context="{'group_by':'metros_especiales'}" />
                    <filter string="Canon de agua propio" name="group_cobro_base_especial"
                        context="{'group_by':'cobro_base_especial'}" />
                </group>
            </search>
        </field>
    </record>
</odoo>